---
"ghost-drift": major
---

Keep `ghost-drift` drift-only: authoring verbs and context emit live in `ghost-scan`, `ghost-drift emit` only accepts `skill`, and drift no longer re-exports the fingerprint authoring API.
