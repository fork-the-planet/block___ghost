---
"ghost-drift": major
"ghost-scan": major
---

Rename Ghost's authored design-language artifact back to `fingerprint.md`, rename the authoring package and CLI to `ghost-scan`, and remove the old expression APIs, filenames, and drift compatibility shims.
